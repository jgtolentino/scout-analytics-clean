# React + Vite starter (TS, Tailwind)
npx create-vite@latest {{project_slug}} --template react-ts
cd {{project_slug}}
npm i tailwindcss postcss autoprefixer zustand react-hook-form axios jwt-decode
npx tailwindcss init -p
# … inject Tailwind config + auth pages