const ejs = require('ejs');                 // templating engine
module.exports = async function scaffold(req) {
  /* 1. pick templates by stack   */
  /* 2. render into dist/<slug>/  */
  /* 3. return paths for deployer */
};